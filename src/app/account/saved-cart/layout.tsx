export const metadata = {
  title: 'Saved Carts - Wishlist | Midas Technical Solutions',
  description: 'Manage your saved shopping carts and add to cart anytime. Access your wishlist and saved items for easy reordering.',
};

export default function SavedCartLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
}
